<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NikeShop" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textViewStyle">@style/NikeShopTextViewStyle</item>
        <item name="android:searchViewStyle">@style/NikeShopSearchStyle</item>
    </style>
    
    
    <style name="NikeShopTextViewStyle" parent="android:Widget.TextView">
        <item name="fontFamily">@font/iranian_sans</item>
    </style>

    <style name="NikeShopSearchStyle" parent="android:Widget.Material.SearchView">
        <item name="title">@string/search</item>
        <item name="endIconDrawable">@drawable/ic_close</item>
        <item name="showAsAction">collapseActionView|ifRoom</item>
    </style>

</resources>